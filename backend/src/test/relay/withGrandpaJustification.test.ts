import * as tap from 'tap';

tap.test('Relay module - withGrandpaJustification method', async (t) => {
  tap.test('should add GRANDPA justification if block does not have any');

  tap.test('should return block as is if there is already justification');

  t.end();
})
